import { DataPageLoading } from "@/components/data-page-loading";

export default function Loading() {
  return <DataPageLoading titleWidth="w-52" subtitleWidth="w-80" cardCount={2} listRows={8} />;
}
