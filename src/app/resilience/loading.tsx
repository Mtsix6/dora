import { DataPageLoading } from "@/components/data-page-loading";

export default function Loading() {
  return <DataPageLoading titleWidth="w-48" subtitleWidth="w-80" cardCount={4} listRows={6} />;
}
