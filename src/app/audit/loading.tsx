import { DataPageLoading } from "@/components/data-page-loading";

export default function Loading() {
  return <DataPageLoading titleWidth="w-40" subtitleWidth="w-80" cardCount={3} listRows={7} />;
}
